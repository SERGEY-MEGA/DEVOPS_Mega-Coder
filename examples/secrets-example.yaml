# Example only. Copy to a private file or create the secret with kubectl commands from BOT_SETUP.md.
apiVersion: v1
kind: Secret
metadata:
  name: mega-coder-alerting-secret
  namespace: mega-coder
type: Opaque
stringData:
  TELEGRAM_BOT_TOKEN: "replace-me"
  TELEGRAM_CHAT_ID: "replace-me"
  TELEGRAM_PARSE_MODE: "HTML"
  ALERTMANAGER_WEBHOOK_SECRET: "replace-me"
  GITLAB_WEBHOOK_SECRET: "replace-me"
