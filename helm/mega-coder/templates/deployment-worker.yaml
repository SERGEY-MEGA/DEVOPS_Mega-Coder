apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mega-coder.fullname" . }}-worker
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: worker
    app.kubernetes.io/name: {{ include "mega-coder.name" . }}
spec:
  # Worker — отдельный компонент приложения, поэтому тоже масштабируется независимо.
  replicas: {{ .Values.replicaCount.worker }}
  selector:
    matchLabels:
      app.kubernetes.io/component: worker
      app.kubernetes.io/name: {{ include "mega-coder.name" . }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: worker
        app.kubernetes.io/name: {{ include "mega-coder.name" . }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      # Worker тоже запускается от фиксированного non-root UID/GID, чтобы kubelet не блокировал контейнер.
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
      containers:
        - name: worker
          # Worker нужен, чтобы показать service-to-service взаимодействие api -> worker в Kubernetes.
          image: {{ include "mega-coder.image" (dict "root" . "image" .Values.images.worker) }}
          imagePullPolicy: {{ .Values.images.worker.pullPolicy }}
          ports:
            - name: http
              containerPort: 8081
          env:
            # Имя сервиса отдаётся в /health и показывает работу ConfigMap в рантайме.
            - name: SERVICE_NAME
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mega-coder.fullname" . }}-config
                  key: WORKER_SERVICE_NAME
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
