# Скопируй в .env и подставь реальные значения (файл .env в git не попадает):
#   cp .env.example .env
#
# Токен — строка вида 123456789:AAH... из @BotFather (не текст «НОВЫЙ_ТОКЕН» из инструкций).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
